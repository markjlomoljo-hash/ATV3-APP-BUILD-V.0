// AcneTrex V3 — shared CutisAI worker contract constants.
//
// Kept free of "server-only" so enqueue helpers and tests can import the
// event type without pulling in the worker runtime.
export const CUTISAI_REPLY_EVENT_TYPE = "cutisai.reply.requested";
