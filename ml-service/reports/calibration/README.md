# Calibration reports

No report exists because no predictive model is trained. Promotion requires held-out reliability analysis, calibration error, decision thresholds, abstention coverage, and subgroup slices tied to immutable artifact and dataset hashes.

