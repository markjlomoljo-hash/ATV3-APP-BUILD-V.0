# AcneTrex local ML runtime

Platform-neutral shared contracts and deterministic runtime selection for the
AcneTrex Expo client and authenticated application gateway. The package ships
no trained model or training data. Native callers provide UUID and artifact
digest adapters so this package does not depend on Node.js built-ins.
